spring:
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: false # false is QueueActiveMQAutoConfiguration ; true is Topic, default is Queue
    template:
      default-destination: my-dest-test